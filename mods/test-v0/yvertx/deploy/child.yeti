module yvertx.deploy.child;

load yvertx;

verticle do {vertx}:
   bus.send vertx "test-handler" {started=true} \(); 
   println "child started";
   \(
   		bus.send vertx "test-handler" {stopped = true} \();
   	);
done;

