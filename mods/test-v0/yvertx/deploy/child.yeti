module yvertx.deploy.child;

load yvertx;

verticle do :
   send "test-handler" {started=true, json=()}; 
   println "child started";
   \(
   		send "test-handler" {stopped = true,json=()};
   	);
done;

