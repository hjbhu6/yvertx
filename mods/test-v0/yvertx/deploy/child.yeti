module yvertx.deploy.child;

load yvertx;
bus = load yvertx.bus;

verticle do {vertx}:
   vertx |> bus.send "test-handler" {started=true} \(); 
   println "child started";
   \(
   		vertx |> bus.send "test-handler" {stopped = true} \();
   	);
done;

