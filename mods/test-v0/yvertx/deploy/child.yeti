module yvertx.deploy.child;

load yvertx;

verticle do :
   send "test-handler" {started=true, for_json=E()}; 
   println "child started";
   \(
   		send "test-handler" {stopped = true,for_json=E()};
   	);
done;

